% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{synpdx_fit}
\alias{synpdx_fit}
\title{Fit NLME models and run interaction tests}
\usage{
synpdx_fit(dat = NULL, control = NULL, drug_a = NULL, drug_b = NULL,
  combo = NULL, B = 100, out_dir = NULL, seed = 990202,
  cutoff = c("auto", "none"), model = "auto",
  select_random_effects = TRUE,
  distance = c("marginal", "residual"),
  selection_order = c("structure_first", "random_first"),
  re_alpha = 0.01, alpha = 0.05, saemix_control = list(),
  verbose = TRUE, data = NULL)
}
\arguments{
\item{dat}{A data.frame with columns \code{subject}, \code{time}, \code{treatment}, and \code{logvol_norm}.}
\item{control}{Control-arm label.}
\item{drug_a}{Drug A monotherapy label.}
\item{drug_b}{Drug B monotherapy label.}
\item{combo}{Combination-arm label.}
\item{B}{Number of bootstrap replicates; 0 skips bootstrap, and values must otherwise be at least 2.}
\item{out_dir}{Optional output directory for CSV and PNG files.}
\item{seed}{Integer random seed.}
\item{cutoff}{\code{"auto"} uses the last common time point, or \code{"none"} uses all available times.}
\item{model}{\code{"auto"} compares the twelve growth/effect forms by BIC, or a model name can be supplied.}
\item{select_random_effects}{Whether to perform boundary-LRT backward random-effect selection.}
\item{distance}{\code{"marginal"} (manuscript pointwise marginal variance) or \code{"residual"}.}
\item{selection_order}{\code{"structure_first"} or \code{"random_first"}.}
\item{re_alpha}{Boundary-LRT threshold for random-effect selection.}
\item{alpha}{Inference level for chi-square and bootstrap summaries.}
\item{saemix_control}{Named options passed to \code{saemix::saemixControl}.}
\item{verbose}{Whether bootstrap progress is printed.}
\item{data}{Alias for \code{dat}; supply either \code{dat} or \code{data}.}
}
\value{A list containing the fitted model, distance results, chi-square result, plot, and bootstrap result.}
\description{Fits nonlinear mixed-effects tumor-growth and drug-effect models, computes the manuscript-consistent interaction distance, and performs chi-square and optional cluster-bootstrap inference.}
\details{Bootstrap refits keep the selected structural model and intentionally use a full diagonal random-effect covariance without repeating random-effect selection.}
\examples{
\dontrun{
df <- read.csv("path/to/your_data.csv", stringsAsFactors = FALSE)
res <- synpdx_fit(
  data = df, control = "control", drug_a = "drug_a",
  drug_b = "drug_b", combo = "combination", B = 1000,
  seed = 990202, cutoff = "auto", model = "auto",
  select_random_effects = TRUE, distance = "marginal",
  out_dir = "synpdx_results")
}
}
